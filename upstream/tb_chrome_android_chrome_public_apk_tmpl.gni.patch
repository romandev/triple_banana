diff --git tb/chrome/android/chrome_public_apk_tmpl.gni tb_diff/chrome/android/chrome_public_apk_tmpl.gni
--- tb/chrome/android/chrome_public_apk_tmpl.gni
+++ tb_diff/chrome/android/chrome_public_apk_tmpl.gni
@@ -135,7 +135,7 @@ template("chrome_public_common_apk_or_module_tmpl") {
   }
 
   target(_target_type, target_name) {
-    forward_variables_from(invoker, "*")
+    forward_variables_from(invoker, "*", [ "disable_library_rename" ])
     if (defined(shared_resources_whitelist_target)) {
       shared_resources_allowlist_target = shared_resources_whitelist_target
       shared_resources_allowlist_locales = shared_resources_whitelist_locales
@@ -283,7 +283,8 @@ template("chrome_public_common_apk_or_module_tmpl") {
       ]
 
       # Adds "crazy." prefix to avoid libraries being extracted when installed.
-      if (use_chromium_linker) {
+      if (use_chromium_linker && !(defined(invoker.disable_library_rename) &&
+                                   invoker.disable_library_rename)) {
         if (!defined(library_renames)) {
           library_renames = []
         }
@@ -373,6 +374,18 @@ template("chrome_public_common_apk_or_module_tmpl") {
 
     deps += [ "//chrome/android:chrome_all_java" ]
 
+    # Use a consistent baseline so that it is easy to regenerate by deleting the
+    # file and re-building the "android_lint" target.
+    if (defined(enable_lint) && enable_lint) {
+      if (!defined(lint_baseline_file)) {
+        lint_baseline_file = "//chrome/android/expectations/lint-baseline.xml"
+      }
+      if (!defined(lint_suppressions_file)) {
+        lint_suppressions_file =
+            "//chrome/android/expectations/lint-suppressions.xml"
+      }
+    }
+
     # Prefer to add this data_dep on the final target instead of java targets
     # like chrome_all_java so that all other targets can build in parallel with
     # lint.
@@ -516,7 +529,9 @@ template("monochrome_public_common_apk_or_module_tmpl") {
                 [ "//chrome/android:monochrome_secondary_abi_lib" ]
             _include_secondary_support = true
             if (invoker.include_64_bit_webview) {
-              shared_libraries += [ "//android_webview:monochrome" ]
+              #if defined(ENABLE_TRIPLE_BANANA)
+              #             shared_libraries += [ "//android_webview:monochrome" ]
+              #endif
               _include_primary_support = true
             }
           }
