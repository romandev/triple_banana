diff --git triple_banana/components/password_manager/core/browser/password_form_manager.h triple_banana_change/components/password_manager/core/browser/password_form_manager.h
index dc792f895a9a..98c676751015 100644
--- triple_banana/components/password_manager/core/browser/password_form_manager.h
+++ triple_banana_change/components/password_manager/core/browser/password_form_manager.h
@@ -26,6 +26,10 @@
 #include "components/password_manager/core/browser/password_store.h"
 #include "components/password_manager/core/browser/votes_uploader.h"
 
+#if defined(ENABLE_TRIPLE_BANANA)
+#include "triple_banana/modules/public/interfaces.h"
+#endif
+
 namespace password_manager {
 
 class FormSaver;
@@ -340,6 +344,11 @@ class PasswordFormManager : public PasswordFormManagerInterface,
   // credentials.
   const std::unique_ptr<FormSaver> form_saver_;
 
+#if defined(ENABLE_TRIPLE_BANANA)
+  // The connection for triplebanana encrypter service
+  mojo::InterfacePtr<encrypter::mojom::EncrypterManager> encrypter_;
+#endif
+
   VotesUploader votes_uploader_;
 
   // |is_submitted_| = true means that a submission of the managed form was seen
-- 
2.23.0

