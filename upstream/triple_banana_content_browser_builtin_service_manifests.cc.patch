diff --git triple_banana/content/browser/builtin_service_manifests.cc triple_banana_change/content/browser/builtin_service_manifests.cc
index 07e2029..62b53e3 100644
--- triple_banana/content/browser/builtin_service_manifests.cc
+++ triple_banana_change/content/browser/builtin_service_manifests.cc
@@ -43,6 +43,8 @@
 #endif  // BUILDFLAG(ENABLE_CROS_LIBASSISTANT)
 #endif  // defined(OS_CHROMEOS)
 
+#include "triple_banana/modules/public/manifest/manifest.h"
+
 namespace content {
 
 namespace {
@@ -79,6 +81,7 @@ const std::vector<service_manager::Manifest>& GetBuiltinServiceManifests() {
           media::GetCdmManifest(),
 #endif
           media::GetMediaManifest(),
+          triple_banana::GetManifest(),
           data_decoder::GetManifest(),
           device::GetManifest(),
           media_session::GetManifest(),
@@ -97,6 +100,7 @@ const std::vector<service_manager::Manifest>& GetBuiltinServiceManifests() {
                         kOutOfProcessBuiltin
                   : service_manager::Manifest::ExecutionMode::
                         kInProcessBuiltin),
+
 #if defined(OS_LINUX)
           font_service::GetManifest(),
 #endif
