diff --git triple_banana/content/browser/builtin_service_manifests.cc triple_banana_change/content/browser/builtin_service_manifests.cc
index 07e2029..57dd7ac 100644
--- triple_banana/content/browser/builtin_service_manifests.cc
+++ triple_banana_change/content/browser/builtin_service_manifests.cc
@@ -43,6 +43,8 @@
 #endif  // BUILDFLAG(ENABLE_CROS_LIBASSISTANT)
 #endif  // defined(OS_CHROMEOS)
 
+#include "triple_banana/modules/public/manifest/manifest.h"
+
 namespace content {
 
 namespace {
@@ -100,6 +102,7 @@ const std::vector<service_manager::Manifest>& GetBuiltinServiceManifests() {
 #if defined(OS_LINUX)
           font_service::GetManifest(),
 #endif
+          triple_banana::GetManifest(),
 #if defined(OS_CHROMEOS)
 #if BUILDFLAG(ENABLE_CROS_LIBASSISTANT)
           // TODO(https://crbug.com/929340): This doesn't belong here!
