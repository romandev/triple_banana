diff --git tb/third_party/blink/renderer/modules/media_controls/media_controls_impl.cc tb_diff/third_party/blink/renderer/modules/media_controls/media_controls_impl.cc
--- tb/third_party/blink/renderer/modules/media_controls/media_controls_impl.cc
+++ tb_diff/third_party/blink/renderer/modules/media_controls/media_controls_impl.cc
@@ -369,6 +369,9 @@ MediaControlsImpl::MediaControlsImpl(HTMLMediaElement& media_element)
           media_element.GetDocument().GetTaskRunner(TaskType::kInternalMedia),
           this,
           &MediaControlsImpl::VolumeSliderWantedTimerFired),
+#if defined(ENABLE_TRIPLE_BANANA)
+      media_dispatcher_(AutoBind(media::mojom::BananaMediaEventDispatcher)),
+#endif
       text_track_manager_(
           MakeGarbageCollected<MediaControlsTextTrackManager>(media_element)) {
   // On touch devices, start with the assumption that the user will interact via
@@ -1863,6 +1866,10 @@ void MediaControlsImpl::OnEnteredFullscreen() {
 
   StopHideMediaControlsTimer();
   StartHideMediaControlsTimer();
+#if defined(ENABLE_TRIPLE_BANANA)
+  if (IsA<HTMLVideoElement>(MediaElement()))
+    media_dispatcher_->OnEnteredVideoFullscreen();
+#endif
 }
 
 void MediaControlsImpl::OnExitedFullscreen() {
@@ -1873,6 +1880,10 @@ void MediaControlsImpl::OnExitedFullscreen() {
   HidePopupMenu();
   StopHideMediaControlsTimer();
   StartHideMediaControlsTimer();
+#if defined(ENABLE_TRIPLE_BANANA)
+  if (IsA<HTMLVideoElement>(MediaElement()))
+    media_dispatcher_->OnExitedVideoFullscreen();
+#endif
 }
 
 void MediaControlsImpl::OnPictureInPictureChanged() {
